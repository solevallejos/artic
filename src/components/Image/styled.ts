import styled from 'styled-components/native';
import {THEME} from '../../../assets/theme/theme';

export const ImageNotAvailable = styled.View`
  border-width: 1px;
  border-color: ${THEME.COLORS.DIVIDERS};
  width: 100%;
  height: 200px;
  background-color: ${THEME.COLORS.DIVIDERS};
  padding: 16px;
`;
